import { Head, Link, usePage } from '@inertiajs/react';
import { BellIcon, CalendarDaysIcon, CurrencyDollarIcon, HomeIcon, UserGroupIcon, UserIcon } from '@heroicons/react/24/outline';
import { PageProps } from '@/types';

export default function Dashboard({ membershipStatus, khairat }: PageProps<{ membershipStatus: string; khairat: { status: string; balanceDue: string } }>) {
    const user = usePage<PageProps>().props.auth.user!;
    const pending = membershipStatus === 'pending';
    return <><Head title="Utama" /><div className="min-h-[100dvh] bg-stone-50 pb-24 text-stone-900 dark:bg-stone-950 dark:text-stone-100"><header className="border-b border-stone-200 bg-white px-4 py-4 dark:border-stone-800 dark:bg-stone-900"><div className="mx-auto flex max-w-lg items-center justify-between"><Link href="/" className="text-lg font-black text-emerald-900 dark:text-emerald-200">Masjid Kariah</Link><Link href={route('profile.edit')} className="grid size-10 place-items-center rounded-full bg-emerald-100 text-emerald-900 dark:bg-emerald-900 dark:text-emerald-100"><UserIcon className="size-5" /></Link></div></header><main className="mx-auto max-w-lg px-4 py-6"><p className="text-sm text-stone-600 dark:text-stone-400">Assalamualaikum,</p><h1 className="mt-1 text-3xl font-black">{user.name}</h1>{pending && <section className="mt-6 rounded-2xl border border-amber-300 bg-amber-50 p-4 text-amber-950"><p className="font-bold">Pendaftaran sedang disemak</p><p className="mt-1 text-sm">Admin masjid akan mengesahkan maklumat kariah anda.</p></section>}<section className="mt-6 rounded-3xl bg-emerald-900 p-6 text-white"><p className="text-sm font-bold text-emerald-200">KHAIRAT KELUARGA</p><div className="mt-3 flex items-end justify-between"><div><p className="text-3xl font-black">RM {khairat.balanceDue}</p><p className="mt-1 text-sm text-emerald-100">Baki semasa · {khairat.status}</p></div><CurrencyDollarIcon className="size-9 text-emerald-200" /></div><button type="button" className="mt-5 w-full rounded-xl bg-white px-4 py-3 text-sm font-bold text-emerald-950">Hantar bukti bayaran</button></section><section className="mt-8"><h2 className="text-xl font-black">Akses pantas</h2><div className="mt-4 grid grid-cols-2 gap-3">{[[CalendarDaysIcon,'Jadual pengajian'],[BellIcon,'Notifikasi'],[UserGroupIcon,'Persatuan'],[CurrencyDollarIcon,'Khairat keluarga']].map(([Icon,label]) => { const ItemIcon = Icon as typeof HomeIcon; return <button type="button" key={label as string} className="rounded-2xl border border-stone-200 bg-white p-4 text-left dark:border-stone-800 dark:bg-stone-900"><ItemIcon className="size-6 text-emerald-800 dark:text-emerald-300" /><p className="mt-6 text-sm font-bold">{label as string}</p></button> })}</div></section></main><nav className="fixed inset-x-0 bottom-0 border-t border-stone-200 bg-white/95 px-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur dark:border-stone-800 dark:bg-stone-900/95"><div className="mx-auto grid max-w-lg grid-cols-5">{[[HomeIcon,'Utama'],[CalendarDaysIcon,'Jadual'],[UserGroupIcon,'Komuniti'],[BellIcon,'Notifikasi'],[UserIcon,'Akaun']].map(([Icon,label], index) => { const ItemIcon = Icon as typeof HomeIcon; return <button type="button" key={label as string} className={`flex flex-col items-center gap-1 py-1 text-[11px] font-semibold ${index === 0 ? 'text-emerald-800 dark:text-emerald-300' : 'text-stone-500'}`}><ItemIcon className="size-5" />{label as string}</button> })}</div></nav></div></>;
}
